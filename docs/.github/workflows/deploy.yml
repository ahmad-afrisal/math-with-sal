name: Deploy VitePress site to Pages

on:
  push:
    branches: [main] # Menjalankan action setiap kali ada push ke branch main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
      - name: Install dependencies
        run: npm install
      - name: Fix Permissions
        run: chmod +x ./node_modules/.bin/vitepress
      - name: Build with VitePress
        run: npm run docs:build # Pastikan script ini ada di package.json
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # POIN PENTING: Jika folder .vitepress ada di dalam folder docs, 
          # maka path-nya harus mengarah ke docs/.vitepress/dist
          path: docs/.vitepress/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
